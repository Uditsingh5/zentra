# Optional: API base URL (without /api). Leave unset for same-origin (proxy in dev, same host in prod).
# VITE_API_URL=https://api.yourapp.com

# Optional: Socket.IO server URL. Leave unset to use current origin.
# VITE_SOCKET_URL=https://yourapp.com

# Optional: Dev server proxy target (default: http://localhost:3000)
# VITE_DEV_API_TARGET=http://localhost:3000
